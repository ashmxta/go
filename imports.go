package main
import ("fmt")
import("math") // can also put in same parenthesis (across different lines)

func main(){
	fmt.Printf("now you have %g problems \n", math.Sqrt(7)) // the fcn names seem to generally start with capital letters
}